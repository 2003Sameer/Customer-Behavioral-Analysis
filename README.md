# Customer-Behavioral-Analysis
This analysis gives us insights about customer behaviour. Who are the regular customers, what they purchase, which category has good sales and which has bad sales ,different age groups people buy what category of products , sales according to gender , age and overall sales analysis and analyze the business problem in sql to gain insights and build a interactive Power Bi Dashboard to visually present the insights

## Project workflow:
1) Data Cleaning using Python (Jupyter Notebook)
2) Advanced Data Analysis and businees analysis using PostgreSQL
3) Data Visualization using Power BI Dashboard

## Dataset
The dataset contains following columns regarding customer shopping behavior:

* Customer ID
* Age group
* Gender
* Product category
* Item purchased
* Purchase amount
* Discount applied
* Shipping type
* Review rating
* Previous purchases
* Subscription status

## Tools & Technologies Used:
1) Jupyter Notebook (For basic data analysis and modification)
2) PgAdmin4 Postgre Sql (For advance analysis and business problem solving)
3) Microsoft Power Bi (For making interactive dashboard displaying insights)

##  Project Stages
### 1. Data Cleaning & Exploration (Python)

In the first stage, the raw dataset was cleaned and prepared using Python.

* Removed duplicate records
* Checked for missing values
* Standardized column formats
* Converted data types
* Performed basic exploratory data analysis
* Prepared the dataset for database storage
* Connect the data with PostgreSQL server for further analysis

### 2. Business Analysis using SQL (PostgreSQL)


* Revenue Analysis
1) What is the total revenue generated by male vs female customers?

``` sql
SELECT 
		gender,
		SUM(purchase_amount) AS Total_Revenue
FROM customer
GROUP BY gender
```

* Customer Spending Behavior
2) Which customers used discounts but still spent more than average?

``` sql
SELECT
		customer_id,
		purchase_amount
FROM customer
WHERE discount_applied = 'Yes' AND purchase_amount >= (SELECT AVG(purchase_amount) AS Avg_Purchase_Amount FROM customer);

```

* Product Performance
3) Which products have the highest average review rating?

``` sql
SELECT
		item_purchased AS Product,
		ROUND(
		AVG(review_rating)::numeric
		,2) AS Average_Review_Rating
FROM customer
GROUP by 1
ORDER BY 2 DESC
LIMIT 5;
```

* Shipping Analysis
4) Do customers spend more when choosing express shipping vs standard shipping?

``` sql
SELECT
		shipping_type,
		ROUND(AVG(purchase_amount),2) AS Average_Purchase_Amount
FROM customer
WHERE shipping_type IN ('Standard','Express')
Group BY shipping_type;
```

* Subscription Impact
5) Do subscribed customers spend more than non-subscribers?

``` sql
SELECT
		subscription_status,
		COUNT(customer_id) AS Total_Customers,
		ROUND(AVG(purchase_amount),2) AS Average_Spend,
		ROUND(SUM(purchase_amount),2) AS Total_Revenue
FROM customer
GROUP BY 1
ORDER BY 4,3 DESC;
```

* Discount Strategy
6) Which products have the highest percentage of purchases with discounts?

``` sql
SELECT
		item_purchased AS Products,
		ROUND(100 * SUM(CASE WHEN discount_applied = 'Yes' THEN 1 ELSE 0 END)/COUNT(*), 2) AS Discount_Rate
FROM customer
GROUP BY 1
ORDER BY 2 DESC
LIMIT 5;
```

* Customer Segmentation
7) Customers were segmented into:
  
a. New customersn

b. Returning customers

c. Loyal customers

d. based on the number of previous purchases.

```sql
WITH customer_type AS 
(
	SELECT 
			customer_id,
			previous_purchases,
		CASE
			WHEN previous_purchases = 1 THEN 'New'
			WHEN previous_purchases BETWEEN 2 AND 10 THEN 'Returning'
			ELSE 'Loyal'
			END AS Customer_Segment
	FROM customer
)
SELECT
		customer_segment,
		COUNT(*) AS Number_of_Customers
FROM customer_type
GROUP BY 1;
)
```

* Product Category Insights
8) Top purchased products within each category

```sql
WITH item_count AS
(
	SELECT 
			category,
			item_purchased,
			COUNT(customer_id) AS Total_Orders,
			ROW_NUMBER() OVER (PARTITION BY category ORDER BY COUNT(customer_id) DESC) AS Item_Rank
	FROM customer
	GROUP BY 1,2
)
SELECT
		Item_Rank,
		category,
		item_purchased,
		Total_Orders
FROM item_count
WHERE Item_Rank <= 3;
```

* Customer Loyalty Insights
9) Are repeat buyers more likely to subscribe?

``` sql
SELECT
		subscription_status,
		COUNT(customer_id) AS Repeat_Buyers
FROM customer
WHERE previous_purchases > 5
GROUP BY 1;
```

* Demographic Insights
10) Revenue contribution by age group

``` sql
SELECT
		age_group,
		SUM(purchase_amount) AS Total_Revenue
FROM customer
GROUP BY 1
ORDER BY 2 DESC;
```

These queries simulate real-world business questions that companies ask their data analysts.

### 3.Power BI Dashboard
The dashboard includes:

* Revenue Insights
* Revenue by gender
* Revenue by age group
* Customer Behavior
* Customer segmentation (New / Returning / Loyal)
* Subscription vs non-subscription analysis
* Product Insights
* Top selling products
* Product category performance
* Discount Analysis
* Impact of discounts on customer purchases
* Shipping Insights
* Comparison between express and standard shipping

The dashboard provides an interactive view of customer purchasing behavior.

### Key Insights

* Certain customer segments contribute significantly more revenue.
* Repeat customers show a higher likelihood of subscription.
* Some products have high ratings but low purchase frequency.
* Discounts influence purchase behavior but do not always reduce revenue.
* Shipping type may influence average purchase amount.
